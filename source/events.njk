---
layout: layouts/main.njk
title: Events | Arnelle Balane
stylesheets:
    - /static/stylesheets/pages/events.css
    - /static/stylesheets/components/items.css
---

<div class="l-Wrapper l-LeftTwoThirds">
    <p>
        Below is a list of my past and upcoming talks and workshops. If you'd like to invite me to give a talk or conduct
        a workshop at your event, please feel free to <a href="mailto:arnellebalane@gmail.com">get in touch</a>.
    </p>
</div>

<section class="Section">
    <div class="l-Wrapper">
        <h1 class="Section__Title">Upcoming Events</h1>

        <ol class="Events">
            {% if (events | filterUpcomingEvents).length > 0 %}
                {%- for event in (events | filterUpcomingEvents) %}
                <li class="Event Item">
                    <h2 class="Item__Name">{% externalLink event.name, event.url %}</h2>
                    <h3 class="Item__Description">{{ event.description }}</h3>

                    <time class="Item__Date" datetime="{{ event.start_date }}">{{ event.start_date }}</time>
                    <data class="Item__Data">{{ event.venue }}</data>
                </li>
                {% endfor -%}
            {% else %}
                <li class="PageDescription NoEvents">
                    <p>I don't have any upcoming events at the moment.</p>
                </li>
            {% endif %}
        </ol>
    </div>
</section>

<section class="Section">
    <div class="l-Wrapper">
        <h1 class="Section__Title">Previous Events</h1>

        <ol class="Events">
            {%- for event in (events | filterPastEvents) %}
            <li class="Event Item">
                <h2 class="Item__Name">{% externalLink event.name, event.url %}</h2>
                <h3 class="Item__Description">{{ event.description }}</h3>

                <time class="Item__Date" datetime="{{ event.start_date }}">{{ event.start_date }}</time>
                <data class="Item__Data">{{ event.venue }}</data>
            </li>
            {% endfor -%}
        </ol>
    </div>
</section>
