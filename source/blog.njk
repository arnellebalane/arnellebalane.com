---
layout: layouts/main.njk
title: Blog | Arnelle Balane
description:
    I experiement a lot with cool new Web technologies and techniques, and I
    write about my experiences with them. Here are what I've written so far.
pagination:
    data: articles
    size: 7
stylesheets:
    - /static/stylesheets/pages/blog.css
    - /static/stylesheets/components/items.css
---

<section class="Section Blog">
    <ol class="Items">
        {% for article in pagination.items %}
        <li class="Item">
            <time class="Item__Date fs-16">{{ article.date | formatDate }}</time>
            <div>
                {% externalLink article.title, article.url, 'Item__Heading fs-24 fw-900' %}
                <p class="Item__Subheading">{{ article.summary }}</p>
                <ul class="Item__Tags fs-16">
                    {% for tag in article.tags %}
                    <li>#{{ tag }}</li>
                    {% endfor %}
                </ul>
            </div>
        </li>
        {% endfor %}
    </ol>
</section>

<section class="Section Pagination">
    {% if pagination.previousPageHref %}
    <a class="Paginator Paginator--previous fs-18" href="{{ pagination.previousPageHref }}">
        <img src="/static/images/icons/left.svg" alt="Previous">
        Newer articles
    </a>
    {% endif %}

    {% if pagination.nextPageHref %}
    <a class="Paginator Paginator--next fs-18" href="{{ pagination.nextPageHref }}">
        Older articles
        <img src="/static/images/icons/right.svg" alt="Next">
    </a>
    {% endif %}
</section>
